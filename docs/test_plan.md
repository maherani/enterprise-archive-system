بر مبنای سند فنی [docs/implementation_plan.md](file:///Ubuntu-26.04/home/alborz/enterprise-archive-system/docs/implementation_plan.md)، تمامی ۶ نیازمندی کلیدی سامانه آرشیو سازمانی مستقر شده و آزمون جامع انتها-به-انتها با موفقیت ۱۰۰٪ پاس شده است.

برای تست زنده این ۶ قابلیت در مرورگر، راهنمای گام‌به‌گام زیر را انجام دهید:

---

### ۱. ورود به سامانه
مرورگر خود را باز کرده و وارد شوید:
👉 **[http://localhost](http://localhost)**

#### اطلاعات ورود دو حساب کاربری:
| نقش | نام کاربری (Username) | رمز عبور (Password) |
| :--- | :--- | :--- |
| **کاربر عادی آرشیو** | `archive_user1` | `User_Password_123!` |
| **مدیر سامانه (ادمین)** | `admin` | `Secure_Admin_Password_123!` |

---

### ۲. سناریوهای تست عملی در مرورگر (مطابق با ۶ بند سند)

#### 🔹 سناریوی ۱: حاکمیت انحصاری پوشه‌ها توسط ادمین (Admin-Only Governance)
1. با کاربر **`archive_user1`** وارد شوید.
2. در بخش **Files**، سعی کنید در ریشه شخصی خود یک پوشه بسازید یا فایلی آپلود کنید (دکمه `+`).
3. **نتیجه:** سیستم بلافاصله با خطای سهمیه دیسک (`Insufficient Storage` / عدم وجود سهمیه شخصی) مانع می‌شود؛ زیرا سهمیه کاربر برای حفظ انضباط روی `0 B` تنظیم شده است.
4. پوشه اشتراکی سازمانی **`Enterprise_Archive`** را باز کنید؛ می‌بینید که کاربر منحصراً درون پوشه‌های هدایت‌شده ادمین اجازه فعالیت دارد.

---

#### 🔹 سناریوی ۲ و ۳: تگ‌گذاری داینامیک، خودکار و آنی (Dynamic Hierarchical Tagging)
1. با همان کاربر **`archive_user1`** به مسیر زیر بروید:
   `/Enterprise_Archive/Finance/2026/Invoices_Archive`
2. یک فایل دلخواه (مثلاً PDF یا عکس) را با Drag & Drop یا دکمه `+` درون این پوشه آپلود کنید.
3. روی فایل کلیک راست کنید (یا آیکون سه‌نقطه `...`) ➔ گزینه **Details** (جزئیات) ➔ زبانه **Tags** (تگ‌ها) را باز کنید.
4. **نتیجه:** ماژول `archive_autotag` به صورت آنی تمامی تگ‌های والد را به فایل الصاق کرده است:
   * `Enterprise_Archive`
   * `Finance`
   * `2026`
   * `Invoices_Archive`

---

#### 🔹 سناریوی ۴: تفکیک تگ‌های حفاظت‌شده سیستمی و تگ‌های کاربر (Tag Protection)
1. در پنل جزئیات فایل، تگ‌های والد (`Finance` یا `2026`) را بررسی کنید؛ علامت ضربدر `x` برای حذف آن‌ها وجود ندارد (چون از نوع `restricted` هستند).
2. در کادر تگ‌ها، یک تگ دلخواه جدید (مثلاً `Draft` یا `Reviewed`) بنویسید و Enter بزنید.
3. **نتیجه:** تگ عمومی شما با موفقیت اضافه می‌شود و در صورت نیاز می‌توانید آن را حذف کنید، در حالی که تگ‌های ساختار آرشیو غیرقابل حذف باقی می‌مانند.

---

#### 🔹 سناریوی ۵: انتشار خودکار تغییر نام پوشه (Folder Rename Propagation)
1. در یک پنجره Incognito مرورگر (یا پس از خروج)، با حساب **`admin`** وارد شوید.
2. به مسیر `/Enterprise_Archive/Finance/2026/` بروید.
3. نام پوشه `Invoices_Archive` را با زدن سه‌نقطه به `Invoices_Verified` تغییر دهید (Rename).
4. به داخل پوشه بروید و جزئیات (Details ➔ Tags) فایلی که قبلاً آپلود کرده بودید را نگاه کنید.
5. **نتیجه:** تگ قبلی `Invoices_Archive` به طور خودکار حذف شده و تگ جدید `Invoices_Verified` جایگزین شده است.

---

#### 🔹 سناریوی ۶: کنترل سقف حجم آپلود به ازای هر کاربر (Per-User Upload Limit)
برای کاربر `archive_user1` سقف آپلود **۱۰ مگابایت** تنظیم شده است:
1. مجدداً با **`archive_user1`** یک فایل زیر ۱۰ مگابایت (مثلاً ۱ مگابایت) آپلود کنید ➔ **موفقیت‌آمیز است**.
2. حالا یک فایل سنگین‌تر از ۱۰ مگابایت (مثلاً یک فایل ویدئویی ۱۲ مگابایتی یا بزرگتر) را برای آپلود انتخاب کنید.
3. **نتیجه:** سیستم مانع از آپلود شده و درخواست را در لایه سرور با خطای دسترسی غیرمجاز (`403 Forbidden`) مسدود می‌کند.


TEST PLAN 2


این طرح تست (Test Plan) به‌صورت گام‌به‌گام به شما نشان می‌دهد ادمین چگونه می‌تواند دسترسی‌های یک کاربر را مدیریت کند و **مجوز ساخت فولدر (یا ایجاد هرگونه آیتم جدید)** را از او سلب کند؛ هم از طریق **رابط کاربری مرورگر (Web UI)** و هم از طریق **خط فرمان (CLI)**.

---

## منطق دسترسی‌ها در سامانه آرشیو (پشت صحنه چیست؟)

سامانه Nextcloud کنترل ساخت فولدر و فایل را در دو لایه اعمال می‌کند:

1. **لایه ریشه شخصی کاربر (Personal Root):** 
   * ادمین سهمیه دیسک شخصی کاربر را با دستور `quota 0` صفر می‌کند. در نتیجه، کاربر در فضای شخصی خود به هیچ وجه اجازه ساخت هیچ فولدری ندارد.
2. **لایه پوشه‌های سازمانی و اشتراکی (Shared/Archive Folders):** 
   * ادمین هنگام اشتراک‌گذاری پوشه با کاربر یا گروه (مثل `Compliance_Unit`)، مجوزهای استاندارد را تعیین می‌کند:
     * **Read (مشاهده):** کاربر فقط فایل‌ها و پوشه‌ها را می‌بیند و می‌خواند.
     * **Create (ایجاد):** اجازه ساخت فایل و فولدر جدید.
     * **Change (ویرایش):** اجازه تغییر محتوای فایل‌ها.
     * **Delete (حذف):** اجازه حذف.
   * **کلید کار:** اگر ادمین تیک **Create** را از کاربر یا گروه بردارد، کاربر دیگر نمی‌تواند هیچ فولدر یا فایلی در آن دایرکتوری ایجاد کند.

---

## گام‌های تست عملی روی مرورگر (گام‌به‌گام)

### مرحله ۱: تست وضعیت فعلی (قبل از سلب مجوز)
1. با مرورگر (پنجره عادی) با حساب **`archive_user1`** (`User_Password_123!`) وارد شوید:
   👉 **[http://localhost](http://localhost)**
2. وارد پوشه اشتراکی `/Enterprise_Archive/Finance/2026/Invoices_Archive` شوید.
3. دکمه **`+` (New)** در بالای صفحه را بررسی کنید؛ مشاهده می‌کنید که گزینه **New folder** (پوشه جدید) برای کاربر فعال است و می‌تواند فولدر بسازد.

---

### مرحله ۲: سلب مجوز ساخت فولدر توسط ادمین در مرورگر

1. در یک پنجره دیگر (مثلاً Incognito / Private) با حساب **`admin`** (`Secure_Admin_Password_123!`) وارد شوید.
2. به بخش **Files** بروید و پوشه **`Enterprise_Archive`** را پیدا کنید.
3. روی آیکون **اشتراک‌گذاری (Share)** در کنار پوشه کلیک کنید تا سایدبار سمت راست باز شود.
4. در زبانه **Sharing**:
   * نام گروه **`Compliance_Unit`** (یا کاربر `archive_user1`) را می‌بینید.
   * روی آیکون **سه‌نقطه `...`** کنار نام گروه یا کاربر کلیک کنید.
5. منوی مجوزها باز می‌شود:
   * تیک گزینه **Can create** (یا Create folder / file) را **بردارید (خاموش کنید)**.
   * *(فقط تیک Read و در صورت نیاز Edit باقی بماند)*.
   * تنظیم بلافاصله و بدون نیاز به ذخیره دستی اعمال می‌شود.

---

### مرحله ۳: بررسی نتیجه توسط کاربر در مرورگر

1. به پنجره مرورگر کاربر **`archive_user1`** برگردید و صفحه را رفرش (`F5`) کنید.
2. وارد پوشه `/Enterprise_Archive/...` شوید.
3. دکمه **`+` (New)** را باز کنید:
   * **نتیجه مشاهده‌شده:** گزینه **New folder** به طور کامل ناپدید شده یا خاکستری و غیرفعال است!
   * اگر کاربر سعی کند فولدری را به درون این صفحه Drag & Drop کند، سامانه با پیام خطا مانع ساخت آن می‌شود.

---

## روش دوم: سلب مجوز از طریق خط فرمان ادمین (CLI / API)

ادمین سیستم بدون نیاز به باز کردن وب نیز می‌تواند این مجوز را از طریق ترمینال لغو کند:

```bash
# مشاهده شناسه اشتراک‌های فعلی
docker compose exec app php occ share:list

# تنظیم سطح دسترسی اشتراک شماره 1 روی مقدار 3 (شامل Read + Update، بدون Create)
# (مقدار قبلی 7 بود که مجوز Create داشت)
docker exec archive_db psql -U nextcloud_user -d nextcloud -c "UPDATE oc_share SET permissions = 3 WHERE id = 1;"
```

---

## سناریوی پیشرفته سازمانی (فقط اجازه آپلود فایل، اما ممنوعیت ساخت فولدر)

در برخی سیستم‌های آرشیو، هدف این است که کاربر بتواند **فایل** آپلود کند، اما **نتواند سرخود پوشه و زیرپوشه جدید بسازد** تا ساختار طبقه‌بندی اسناد دست‌نخورده بماند.

* این کار در سطح پروتکل WebDAV با مسدودسازی متد **`MKCOL`** (Make Collection) در ماژول بومی آرشیو (`archive_autotag`) برای کاربران غیر ادمین انجام می‌شود؛ یعنی:
  * متد `PUT` (آپلود سند) ➔ مجاز
  * متد `MKCOL` (ساخت پوشه) ➔ خطای `403 Forbidden`

اگر مایل باشید، می‌توانیم این سناریوی پیشرفته را نیز به‌عنوان یک قابلیت فعال کنیم تا کاربر امکان ساخت هیچ زیرپوشه‌ای نداشته باشد اما آپلود فایل‌هایش فعال بماند.



---

# TEST PLAN 3: ??? ??? ????? ?????? ??????? ? ?????????? ????? (ACL & Tag Isolation)

??? ??? ??? ?? ???? ??????? ?????? ???? ?????? ????? ????? ??? ??? ?? ????? ??? ??? ?????? ??? ??? ?????? ?? ???????? ????? ?? ?????? ????? ???? ?????? ?????? ????? ??? ????? ????? ??????? ???? ?? ?? ???? ???? ????.

---

## ??????? ??? ? ?????? ??????

| ??? / ????? | ???????? ???? ?????? | ?????? ???? ?????? | ???????? ??? ? ????? |
| :--- | :--- | :--- | :--- |
| **????? ???? (????? `archive_user1`)** | ??? ???????? ???????? ???? ???? + ????????? ?? ????? ?? ?? Grant ???? ??? | ??? ?????? ????????? ???? ???? + ?????? ??????/????? | ??? ??????? ? ?????? ????? ???? |
| **????? ??? (????? `api_worker`)** | ??? ???????? ???????? ???? ???? + ????????? ?? ????? ?? ?? Grant ???? ??? | ??? ?????? ????????? ???? ???? + ?????? ??????/????? | ??? ??????? ? ?????? ????? ???? |
| **???? ????? (`admin`)** | ???? ???? ???????? ???????? ?? ???? ?????? ????? | ???? ???? ?????? ????? ?? ????? | ?????? ??? ?? ???? ?????? ?? ????? ???? ?? ???? ?? ?? |

---

## ????????? ??? ??????????

### ??????? ?: ??? ???? ? ??? ?????? ????? ??? ?? ???????? ????? ???
1. ????? `archive_user1` ?? ??? ??????? ?? ??? `confidential_doc.pdf` ?? ???? `/Enterprise_Archive/Finance/2026/Invoices_Archive` ????? ??????.
2. ????? `api_worker` ???? ???? ???? ??????:
   - **????? ?? ???? ???? (Web UI ? WebDAV PROPFIND):** ???? `confidential_doc.pdf` ????? ?? ???? ???? ??????? (?????????? ????).
3. ????? `api_worker` ???????? ???? ?????? ???? ?? ???????? ??????:
   - **?????:** ?????? ???? `HTTP 404 Not Found` ???????????.
4. ????? `api_worker` ???? ?????? ???? ?? ??? ???:
   - **?????:** ?????? ?? ???? `HTTP 404 Not Found` ???? ??????.

---

### ??????? ?: ???? ? ?????? ???? ????? ?? ???????? ????????
1. ????? `admin` ???? ???? `/Enterprise_Archive/Finance/2026/Invoices_Archive` ??????.
2. **?????:** ????? ???? `confidential_doc.pdf` ?? ?? ????? ???? ?????? ?????? ???? ? ?? ????? ???????? ?? ?? ?????? (`HTTP 200`) ?? ??? ???.

---

### ??????? ?: ????? ?????? ???? ????? ? ??? ??? ?????? (Grant & Revoke)
1. ????? ?? ???? ?? ????? ?? ????? `api_worker` ??? ???? ????? `440` ???? ?????? ??????:
   ```bash
   docker exec -u www-data archive_app php occ archive:file:grant grant 440 api_worker
   ```
2. ????? `api_worker` ???? ?? ???????? (Refresh) ??????:
   - **?????:** ???? ????? ???? ?? ???? ??? ? ???????? ?? ?? ?? ?????? ?????? ??? (`HTTP 200`).
3. ????? ?????? ??????? ?? ??? ??????:
   ```bash
   docker exec -u www-data archive_app php occ archive:file:grant revoke 440 api_worker
   ```
4. ????? `api_worker` ?????? ???? ?? ?????? ?? ?????? ???? ??????:
   - **?????:** ?????? ????? ??? ??? ? ???? ?????? ????? ? ???? `HTTP 404` ???? ??????.

---

### ??????? ?: ?????????? ?????? ?????
1. ????? `archive_user1` ?? ?? ????? ?? ??? `User1_Private` ????? ??????.
2. ????? `archive_user1` ? ????? `admin` ??? ?? ?? ?? ???? ????? ? ??????? ????????.
3. ????? `api_worker` ???? ????? ?? ??? ??????:
   - **?????:** ?? `User1_Private` ????? ???? ?? ???? ????? ? ?? ???? ??????? ??? ?? ?????? ??????? ?? ???? `404 Not Found` ????? ??????.

---

### ??????? ?: ?????? ????? ?????? ??????
1. ?????? ???????????? ????? `Enterprise_Archive`? `Finance`? `2026` ? `Invoices_Archive` ?? ???? ????? ?? ????? ????? ???????? ???? ???? ??????? ???? ?????? ? ????? ?????.

---

### ??????? ?: ????? ??? ?????? ??????? (Automated Test Suite)
???? ????? ????? ?????? ???? ????????? ???:
```bash
python3 tests/test_archive_acl_and_tag_isolation.py
```
**????? ???? ??????:**
`ALL 7 ACL & TAG ISOLATION TESTS PASSED WITH 100% SUCCESS!`
